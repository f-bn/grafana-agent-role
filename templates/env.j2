# Managed by Ansible

# Any user defined arguments
CUSTOM_ARGS="{{ grafana_agent_custom_args | join(' ') }}"

# Restart on system upgrade. Default to true
RESTART_ON_UPGRADE={{ grafana_agent_restart_on_upgrade | lower }}

# Custom environment variables
{% for var in grafana_agent_custom_env %}
{{ var }}
{% endfor %}
